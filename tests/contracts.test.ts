import {describe,it,expect} from 'vitest';import validate from '../src/generated/config.js';import snapshot from '../src/generated/snapshot.js';import {settingsSchema} from '../src/lib/contracts';
const config={version:1,api_id:'',phone:'',base_url:'https:'+'//api.openai.com/v1',model:'gpt-4o-mini',wpm:105,words:8,live_enabled:false,active_preset:'natural',presets:[{id:'natural',name:'Естественный',prompts:['Кратко.']}]};
const state={protocol:2,config:{...config,live_calibrated:false,live_device:null,has_api_key:false,has_api_hash:false,warning:''},stage:'idle',detail:'Остановлено',prepared:false,active:false,chats:[],target:null,profile:null,hotkeys:true,telemetry:[]};
describe('JSON Schema boundary',()=>{it('accepts config',()=>expect(validate(config)).toBe(true));it('requires live setting',()=>{const{live_enabled:_,...rest}=config;expect(validate(rest)).toBe(false)});it('accepts snapshot',()=>expect(snapshot(state)).toBe(true));it('rejects protocol drift',()=>expect(snapshot({...state,protocol:1})).toBe(false));it('rejects raw HWID',()=>expect(snapshot({...state,config:{...state.config,device_hwid:'private'}})).toBe(false));});
describe('form validation',()=>{const v={...config,api_key:'',api_hash:''};it('accepts values',()=>expect(settingsSchema.safeParse(v).success).toBe(true));it('rejects external HTTP',()=>expect(settingsSchema.safeParse({...v,base_url:'http:'+'//example.org'}).success).toBe(false));it('accepts localhost',()=>expect(settingsSchema.safeParse({...v,base_url:'http:'+'//127.0.0.1:8080/v1'}).success).toBe(true));});
